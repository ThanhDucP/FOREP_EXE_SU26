spring:
  application:
    name: forep-exe
  datasource:
    url: ${DATABASE_URL:jdbc:h2:mem:forep;MODE=PostgreSQL;DATABASE_TO_LOWER=TRUE;DB_CLOSE_DELAY=-1}
    username: ${DATABASE_USERNAME:sa}
    password: ${DATABASE_PASSWORD:}
  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false
  flyway:
    enabled: true
  mail:
    host: ${MAIL_HOST:smtp.gmail.com}
    port: ${MAIL_PORT:587}
    username: ${MAIL_USERNAME:}
    password: ${MAIL_APP_PASSWORD:}
    properties:
      mail.smtp.auth: true
      mail.smtp.starttls.enable: true
server:
  port: ${SERVER_PORT:8080}

forep:
  jwt:
    secret: ${JWT_SECRET:change-me-change-me-change-me-change-me}
    expiration-minutes: ${JWT_EXPIRATION_MINUTES:1440}
  cors:
    allowed-origins: ${CORS_ALLOWED_ORIGINS:https://forep-ai.vercel.app,http://localhost:3000,http://localhost:3001}
  payos:
    config-encryption-key: ${PAYOS_CONFIG_ENCRYPTION_KEY:}
    pending-monitor-delay-ms: ${FOREP_PAYOS_PENDING_MONITOR_DELAY_MS:2000}
    pending-monitor-initial-delay-ms: ${FOREP_PAYOS_PENDING_MONITOR_INITIAL_DELAY_MS:2000}
    pending-monitor-batch-size: ${FOREP_PAYOS_PENDING_MONITOR_BATCH_SIZE:20}
    pending-monitor-lookback-hours: ${FOREP_PAYOS_PENDING_MONITOR_LOOKBACK_HOURS:24}
  mail:
    from-name: ${MAIL_FROM_NAME:FOREP EXE}
  ai:
    service-url: ${AI_SERVICE_URL:}
    service-token: ${AI_SERVICE_TOKEN:}
    connect-timeout-millis: ${AI_SERVICE_CONNECT_TIMEOUT_MILLIS:3000}
    read-timeout-millis: ${AI_SERVICE_READ_TIMEOUT_MILLIS:10000}
    max-concurrent-requests: ${AI_SERVICE_MAX_CONCURRENT_REQUESTS:4}
    acquire-timeout-millis: ${AI_SERVICE_ACQUIRE_TIMEOUT_MILLIS:1000}
    dedupe-wait-millis: ${AI_SERVICE_DEDUPE_WAIT_MILLIS:14000}
    retry-after-seconds: ${AI_SERVICE_RETRY_AFTER_SECONDS:15}
    circuit-breaker-failure-threshold: ${AI_SERVICE_CIRCUIT_BREAKER_FAILURE_THRESHOLD:5}
    circuit-breaker-open-millis: ${AI_SERVICE_CIRCUIT_BREAKER_OPEN_MILLIS:60000}
